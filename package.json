{
  "name": "arkus-scraper-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server/index.js",
    "start": "node server/index.js",
    "postinstall": "npx --yes playwright install --with-deps chromium",
    "health": "node -e \"import('node:http').then(({createServer})=>createServer((_,r)=>r.end('ok')).listen(8080))\"",
    "test": "node --test --test-reporter=spec"
  },
  "dependencies": {
  "express": "^4.19.2",
  "cors": "^2.8.5",
  "playwright": "^1.55.0",
  "@supabase/supabase-js": "^2.54.0"
}
}